{
  "name": "note",
  "version": "1.0.0",
  "dependencies": {
  },
  "scripts": {
    "build": "rm -rf docs && gitbook build . docs",
    "serve": "rm -rf docs && gitbook serve . docs",
    "publish": "rm -rf _book && git add -u && git commit -m publish && git push"
  }
}
